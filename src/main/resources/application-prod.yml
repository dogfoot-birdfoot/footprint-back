spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:mysql://localhost:3306/footprint
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

cloud:
  aws:
    credentials:
      access-key: AKIA4MTWIRLOG5UFK2DP
      secret-key: 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
    s3:
      bucket: dfbf-footprint

logging:
  level:
    'org.hibernate.SQL': debug
    'org.hibernate.type': trace